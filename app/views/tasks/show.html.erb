<h1>SHOW PAGE</h1>
<h4>tHiS iS a ShOw PaGe</h4>
<h2><%= @task.title %></h2>
<h3><%= @task.description %></h3>
<p>
 <%= link_to 'Add subtask', new_task_subtasks_path(@task) %>
</p>
<h4><%= link_to 'index page', tasks_path%></h4>
<h3><%=  %>

<table>
    <thead>
        <th>#</th>
        <th>User Story</th>
        <th>Story Points</th>
        <th>Status</th>
    </thead>
    <tbody>
        <% @task.subtasks.each do |task|%>
            <tr>
                <td><%= task.id%></td>
                <td><%= task.user_story%></td>
                <td><%= task.user_points%></td>
                <td><%= task.status %></td>
            </tr>
        <% end%>
    </tbody>
</table>